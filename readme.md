Kotlin / ktor port of a Scala / http4s backend demo app.

run with

`-Dconfig=local` or `-Dconfig=dev`

or use ```./bin/startLocal.sh```

Endpoints:

`/`
`/api/badgers`
`/api/badgers/{badger name}`  
`/status`  
`/status/json`  
`/status/version`  
`/metrics`